package pub.qiuf.litemc.protocol.play.client;

import pub.qiuf.litemc.common.bean.datatype.Packet;
import com.google.common.io.ByteArrayDataInput;
import com.google.common.io.ByteStreams;
import pub.qiuf.litemc.common.util.ByteUtils;
import pub.qiuf.litemc.common.annotation.MCType;
import com.google.common.io.ByteArrayDataOutput;
import pub.qiuf.litemc.common.annotation.ServerPacket;
import lombok.Data;
import pub.qiuf.litemc.common.util.StreamUtils;

/**
 * Code is generated by <code>pub.qiuf.litemc.generator.ProtocolGenerator</code>, Please don't modify.<br>
 * See more detail from <a>https://wiki.vg/Protocol</a><br>
 */
@Data
@ServerPacket(0x57)
public class Play_Client_EntityTeleport extends Packet {
    /** Packet ID */
    @MCType("VarInt")
    private int packetId = 0x57;
    @MCType("VarInt")
    private int entityId;
    @MCType("Double")
    private double x;
    @MCType("Double")
    private double y;
    @MCType("Double")
    private double z;
    /** New angle, not a delta */
    @MCType("Angle")
    private byte yaw;
    /** New angle, not a delta */
    @MCType("Angle")
    private byte pitch;
    @MCType("Boolean")
    private boolean onGround;

    @Override
    public byte[] encode() throws Exception {
        ByteArrayDataOutput packet = ByteStreams.newDataOutput();
        // Packet ID
        packet.write(ByteUtils.encodeVarInt(packetId));
        packet.write(ByteUtils.encodeVarInt(entityId));
        packet.write(ByteUtils.encodeDouble(x));
        packet.write(ByteUtils.encodeDouble(y));
        packet.write(ByteUtils.encodeDouble(z));
        // New angle, not a delta
        packet.write(ByteUtils.encodeAngle(yaw));
        // New angle, not a delta
        packet.write(ByteUtils.encodeAngle(pitch));
        packet.write(ByteUtils.encodeBoolean(onGround));
        return packet.toByteArray();
    }
    @Override
    public void decode(ByteArrayDataInput in) throws Exception {
        // Packet ID
        packetId = StreamUtils.readVarInt(in);
        entityId = StreamUtils.readVarInt(in);
        x = StreamUtils.readDouble(in);
        y = StreamUtils.readDouble(in);
        z = StreamUtils.readDouble(in);
        // New angle, not a delta
        yaw = StreamUtils.readAngle(in);
        // New angle, not a delta
        pitch = StreamUtils.readAngle(in);
        onGround = StreamUtils.readBoolean(in);
    }
    public static class Play_Client_EntityTeleport_Builder {
        private Play_Client_EntityTeleport object = new Play_Client_EntityTeleport();
        /** Packet ID */
        public Play_Client_EntityTeleport_Builder packetId(int packetId) {
            object.packetId = packetId;
            return this;
        }
        public Play_Client_EntityTeleport_Builder entityId(int entityId) {
            object.entityId = entityId;
            return this;
        }
        public Play_Client_EntityTeleport_Builder x(double x) {
            object.x = x;
            return this;
        }
        public Play_Client_EntityTeleport_Builder y(double y) {
            object.y = y;
            return this;
        }
        public Play_Client_EntityTeleport_Builder z(double z) {
            object.z = z;
            return this;
        }
        /** New angle, not a delta */
        public Play_Client_EntityTeleport_Builder yaw(byte yaw) {
            object.yaw = yaw;
            return this;
        }
        /** New angle, not a delta */
        public Play_Client_EntityTeleport_Builder pitch(byte pitch) {
            object.pitch = pitch;
            return this;
        }
        public Play_Client_EntityTeleport_Builder onGround(boolean onGround) {
            object.onGround = onGround;
            return this;
        }
        public Play_Client_EntityTeleport build() {
             return object;
        }

    }
    public static Play_Client_EntityTeleport_Builder builder() {
        return new Play_Client_EntityTeleport_Builder();
    }
}